The second version of Anagram using string literals instead of string types.
To run simply take all files from anagram2, put them into a workspace using visual studio code that
has a rust extension and cargo build then cargo run.
